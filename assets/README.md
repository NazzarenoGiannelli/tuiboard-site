# Screenshots

Drop real tuiboard screenshots here with these exact filenames. Until a file
exists, the page shows a tidy `[ screenshot: … ]` placeholder, so the site is
presentable empty.

| File | Section | What to capture |
|------|---------|-----------------|
| `shot-kanban.png`  | 01 · the board | The kanban board — columns + cards. The full-dashboard shot works too. |
| `shot-planner.png` | 02 · planner   | The Today / Tomorrow planner zone. (Lowest priority — placeholder is fine.) |
| `shot-agenda.png`  | 03 · agenda    | The 24h agenda with calendar events overlaid (flagship — the v0.8 write feature). |
| `shot-agents.png`  | 04 · agents    | The live Claude Code sessions panel. |
| `og.png`           | social preview | 1200×630 — the full dashboard is perfect here. |

Tips: shoot at a wide terminal (≥150 cols) so all zones show; the frames are
~16:10, so crop roughly to that. PNG preferred. Keep each under ~600 KB if you
can (run them through an optimizer); they're lazy-loaded regardless.
